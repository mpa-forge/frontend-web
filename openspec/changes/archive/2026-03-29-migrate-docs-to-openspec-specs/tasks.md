## 1. Capture capability specs

- [x] 1.1 Create delta specs for the current frontend runtime, local stack, and
      container runtime behavior
- [x] 1.2 Review the migrated requirements and scenarios to ensure they preserve
      the current repo behavior without expanding scope

## 2. Add compatibility entrypoints

- [x] 2.1 Add lightweight `docs/` summaries that point to the canonical OpenSpec
      capabilities
- [x] 2.2 Update repo entrypoints such as `README.md` so the canonical
      OpenSpec-versus-compatibility-doc split is explicit

## 3. Restructure agent context

- [x] 3.1 Update `AGENTS.md` so only repo-local context plus the shared common
      agent context is always loaded
- [x] 3.2 Reclassify broader shared planning docs as conditional references and
      shared workflows as explicit skills

## 4. Finalize the migration

- [x] 4.1 Validate the OpenSpec change and repo documentation updates
- [x] 4.2 Archive the completed change so the canonical specs are written under
      `openspec/specs/`
